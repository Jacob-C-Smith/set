// Standard library
#include <stdio.h>
#include <stdlib.h>

// Include
#include <set/set.h>

// Entry point
int main ( int argc, const char* argv[] )
{

    // Initialized data
    set *p_set = (void *) 0;

    // Success
    return EXIT_SUCCESS;
}